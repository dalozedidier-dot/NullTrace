# NullTrace
A minimal, reproducible, and CI-stable baseline for deterministic soak testing and artifact generation. This repository intentionally follows a strict src-only layout and contains only two workflows: CI and mass soak tests. No hidden automation, no implicit behavior, no legacy scripts.
